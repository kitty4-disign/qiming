# DeepTutor 本机 Docker 部署步骤

> 适用：仓库已克隆在 `D:\ai\DeepTutor`。本文件是给你在本机（已装并启动 Docker）直接跑的命令清单。

## 前置条件
- 本机已安装并**启动 Docker Desktop**（Windows）或 docker + docker compose（Linux/macOS）。
  - 验证：`docker info` 能正常输出（本沙箱里这一步被拒，所以是在你自己的机器上执行）。
- Python（用来跑官方 compose 包装脚本 `scripts/docker_compose.py`；不跑脚本也可直接用 `docker compose`）。

## 方式一：官方预构建镜像（推荐，免构建，最快）
自动从 `ghcr.io/hkuds/deeptutor:latest` 拉镜像，单容器，约 1 分钟起服务。
```bash
cd D:\ai\DeepTutor
cp .env.example .env            # 已为你生成，可跳过
python scripts/docker_compose.py -f docker-compose.ghcr.yml up -d
# 或等价： docker compose -f docker-compose.ghcr.yml up -d
```
浏览器打开 http://localhost:3782

## 方式二：源码构建（用于二次开发，改完代码需重新 build）
会构建 `Dockerfile`(production) + `Dockerfile.runner`(沙箱侧车) + 可选 PocketBase，首次构建较慢（Next.js 前端 + Python 依赖）。
```bash
cd D:\ai\DeepTutor
cp .env.example .env
python scripts/docker_compose.py up -d
```

## 配置模型（关键：不用在 .env 填 Key！）
DeepTutor 的 LLM provider **不在 `.env` 配置**，而是在启动后的**网页 Settings 页面**或 `data/user/settings/model_catalog.json` 里配置。
1. 启动后打开 http://localhost:3782 → Settings → 添加 provider。
2. 云端：填 OpenAI 兼容端点 + API Key。
3. 本地模型（无需 Key）：Ollama / LM Studio / vLLM，base_url 填 `http://host.docker.internal:11434`（Ollama 默认端口）。
- 不填 provider 也能起服务，只是还不能对话；配好即可。

## 端口
- 前端 3782，后端 8001（默认；被占用可在 `.env` 改 `HOST_PORT_*` 或 compose 的 `DEEPTUTOR_DOCKER_*_PORT`）。

## 数据持久化
- 所有数据在 `./data`（用户工作区、记忆、知识库），已挂载 volume，容器删除不丢。

## 远程 / 给别人部署
- 若在服务器部署、别人从自己电脑访问：在 `data/user/settings/system.json` 设 `next_public_api_base_external` 为服务器公网 IP，否则浏览器会去连自己本机的 8001 而失败。
- 分发你二开后的版本：见 `D:\ai\二开成果分发部署指南.md`。

## 当前进度 / 阻塞
- ✅ 已在本环境 `git clone` 仓库到 `D:\ai\DeepTutor`（90M），并生成 `.env`。
- ⛔ 本沙箱未运行 Docker daemon，且 Docker 访问权限被拒绝，**无法在此处实际 `up`**。
- 👉 请在你已启动 Docker Desktop 的本机上执行上面「方式一」三条命令即可。
