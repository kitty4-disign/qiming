# DeepTutor 本机 Docker 部署步骤

> 适用：已克隆本仓库（`kitty4-disign/qiming`，feature/k12-competition-v2 分支）。
>
> 本文件提供两种 Docker 部署方式：上游官方镜像（不含 K12 二开代码）和本地源码构建（包含全部 K12 改动）。**竞赛演示必须使用方式二（本地源码构建）**，方式一仅供快速体验上游原始功能。

## 前置条件

| 项目 | 要求 |
|---|---|
| Docker | Docker Desktop 24+（Windows/macOS）或 docker + docker compose（Linux） |
| Python | 3.11+（用于运行 compose 包装脚本，不跑脚本也可直接用 `docker compose`） |
| Node.js | 20+（仅本地源码部署需要，用于前端构建） |
| 磁盘空间 | ≥ 5GB（镜像构建 + 数据） |

验证 Docker 已启动：

```bash
# PowerShell / Bash 通用
docker info
```

## 方式一：上游官方镜像（不含 K12 二开代码）

> ⚠️ **警告**：此方式拉取的是上游 `ghcr.io/hkuds/deeptutor:latest` 镜像，**不包含本仓库的 K12 竞赛改动**。仅适合快速体验 DeepTutor 原始功能。**竞赛演示请使用方式二。**

```bash
# PowerShell
cd <仓库根目录>
Copy-Item .env.example .env -Force    # 已存在可跳过
python scripts/docker_compose.py -f docker-compose.ghcr.yml up -d

# Bash (Linux/macOS)
cd <仓库根目录>
cp .env.example .env
python scripts/docker_compose.py -f docker-compose.ghcr.yml up -d
```

浏览器打开 http://localhost:3782

## 方式二：本地源码构建（竞赛演示用，包含全部 K12 改动）✅ 推荐

构建 `Dockerfile`(production) + `Dockerfile.runner`(沙箱侧车) + 可选 PocketBase。首次构建较慢（Next.js 前端 + Python 依赖），后续修改代码后需重新 build。

```bash
# PowerShell
cd <仓库根目录>
Copy-Item .env.example .env -Force
python scripts/docker_compose.py up -d

# Bash (Linux/macOS)
cd <仓库根目录>
cp .env.example .env
python scripts/docker_compose.py up -d
```

> `<仓库根目录>` 替换为你本机的实际克隆路径，例如 `D:\ai\DeepTutor` 或 `/home/user/DeepTutor`。

## 配置模型（关键：不用在 .env 填 Key！）

DeepTutor 的 LLM provider **不在 `.env` 配置**，而是在启动后的**网页 Settings 页面**或 `data/user/settings/model_catalog.json` 里配置。

1. 启动后打开 http://localhost:3782 → Settings → 添加 provider。
2. 云端：填 OpenAI 兼容端点 + API Key。
3. 本地模型（无需 Key）：Ollama / LM Studio / vLLM，base_url 填 `http://host.docker.internal:11434`（Ollama 默认端口）。
4. **K12 需要 embedding 模型**：必须配置至少一个 embedding 模型（不是 chat 模型），否则知识库索引会失败。
   - Agnes 网关用户：`base_url` 通常为 `https://apihub.agnes-ai.com/v1/embeddings`，`send_dimensions=false`。
- 不填 provider 也能起服务，只是还不能对话和索引知识库；配好即可。

## 初始化 K12 知识库（竞赛演示必需）

模型配置完成后，初始化四个 K12 阶段知识库：

```bash
# 方式一：脚本
python scripts/init_k12_knowledge_bases.py

# 方式二：CLI
deeptutor kb init-k12

# 强制重建（已有索引也重建）
python scripts/init_k12_knowledge_bases.py --force

# 仅复制源文件，不索引（无 embedding 模型时预置素材）
python scripts/init_k12_knowledge_bases.py --skip-index
```

成功后应看到四个知识库状态为 `ready`：

```bash
# 验证
curl http://localhost:8001/api/v1/knowledge/list
```

应包含 `k12-ai-primary-lower`、`k12-ai-primary-upper`、`k12-ai-middle`、`k12-ai-high`，状态为 `ready`。

## 赛前自检（推荐）

部署完成后运行自检脚本，确认所有必需项就绪：

```bash
python scripts/k12_preflight.py
```

退出码 0 = 全部必需项通过；非 0 = 有必需项未通过。详细说明见 `docs/k12-deployment.md`。

## 端口

| 服务 | 默认端口 | 环境变量 |
|---|---|---|
| 前端 | 3782 | `HOST_PORT_FRONTEND` 或 `DEEPTUTOR_DOCKER_FRONTEND_PORT` |
| 后端 | 8001 | `HOST_PORT_BACKEND` 或 `DEEPTUTOR_DOCKER_BACKEND_PORT` |

端口冲突时修改 `.env` 中的端口变量。

## 数据持久化

- 所有数据在 `./data`（用户工作区、记忆、知识库），已挂载 volume，容器删除不丢。
- K12 知识库索引在 `./data/knowledge_bases/`。
- 学生画像在 `./data/user/settings/education_profile.json`。
- 学习事件在 `./data/user/settings/education_events.json`。

## 沙箱配置

K12 编程实验室需要沙箱执行学生代码。沙箱后端优先级：

1. **RunnerSidecar**（Docker 部署推荐）：通过 `Dockerfile.runner` 构建，自动随 `docker compose up` 启动。
2. **Bwrap**（Linux 原生）：需要 Linux 命名空间权限。
3. **RestrictedSubprocess**（兜底）：无命名空间权限时使用，隔离强度较弱。

沙箱不可用时，编程实验室返回 HTTP 503（不伪造成功），其他功能不受影响。

## 清理与重建

```bash
# 停止并删除容器（保留数据）
python scripts/docker_compose.py down

# 停止并删除容器和数据（⚠️ 会丢失知识库索引和学习记录）
python scripts/docker_compose.py down -v

# 重新构建（修改代码后）
python scripts/docker_compose.py up -d --build

# 查看日志
python scripts/docker_compose.py logs -f
```

## 端口冲突排查

```bash
# 查看占用端口的进程（Windows）
netstat -ano | findstr :3782
netstat -ano | findstr :8001

# 查看占用端口的进程（Linux/macOS）
lsof -i :3782
lsof -i :8001

# 修改端口
# 编辑 .env，修改 HOST_PORT_FRONTEND 和 HOST_PORT_BACKEND
```

## 远程部署

若在服务器部署、别人从自己电脑访问：

1. 在 `data/user/settings/system.json` 设 `next_public_api_base_external` 为服务器公网 IP。
2. 否则浏览器会去连自己本机的 8001 而失败。
3. 确保服务器防火墙开放 3782 和 8001 端口。

## 演示种子数据

竞赛演示前可一键创建演示画像和初始化知识库：

```bash
# 创建演示画像 + 初始化知识库
python scripts/k12_demo_seed.py

# 仅创建演示画像
python scripts/k12_demo_seed.py --profile-only

# 重置演示学习进度（⚠️ 破坏性操作，需明确参数）
python scripts/k12_demo_seed.py --reset-demo-progress
```

## 完整部署文档

更详细的部署指南（含本地源码部署、API 速查、常见问题排查）见 `docs/k12-deployment.md`。
